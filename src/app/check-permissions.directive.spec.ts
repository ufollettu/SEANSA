import { CheckPermissionsDirective } from './check-permissions.directive';

describe('CheckPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
